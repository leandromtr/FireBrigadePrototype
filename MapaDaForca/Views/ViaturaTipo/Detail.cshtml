@model ViaturaTipo
@{
    ViewBag.Title = "Tipo da Viatura: " + Model.Sigla;
    ViewBag.Breadcrumbs = new List<Breadcrumb>()
{
new Breadcrumb() { Name = "Dashboard", Link = "/" },
new Breadcrumb() { Name = "Tipo da Viatura", Link = @Url.Action("Index","ViaturaTipo")},
new Breadcrumb() { Name = Model.Sigla, Link = @Url.Action("Detail","ViaturaTipo", new { @id = Model.Id }), IsActive = true}
};
    ViewData["DeleteButtonLink"] = Url.Action("Delete", "ViaturaTipo", new { @id = Model.Id });
}



<div class="row">
    <div class="col-lg-6">
        <div class="card">
            <div class="card-body">
                <form id="form">
                    @Html.HiddenFor(x => x.Id)

                    <div class="form-group">
                        <label class="form-label">@Html.DisplayNameFor(m => m.Sigla)</label>
                        @Html.TextBoxFor(m => m.Sigla, new { @class = "form-control", @type = "text", @required = "required" })
                    </div>

                    <div class="form-group">
                        <label class="form-label">@Html.DisplayNameFor(m => m.Descricao)</label>
                        @Html.TextBoxFor(m => m.Descricao, new { @class = "form-control", @type = "text", @required = "required" })
                    </div>
                </form>
                <div class="form-group mb-0">
                    <div>

                        <button id="SaveViaturaTipo" class="btn btn-primary waves-effect waves-light">Salvar</button>
                        <button type="reset" class="btn btn-secondary waves-effect m-l-5" onclick="location.href='@Url.Action("Index", "ViaturaTipo")'">Cancelar</button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>




<script type="text/javascript">
    $(document).ready(function () {

        $("#SaveViaturaTipo").click(function (e) {

            e.preventDefault();

            $.ajax({
                url: '@Url.Action("Edit","ViaturaTipo")',
                type: "POST",
                datatype: "json",
                data: $("#form").serialize()
            })
            .done(function (data) {
                if (data.success == true) {
                    $.notify(data.message, "success");
                }
                else {
                    $.notify(data.message, "error");
                }
            })
        });



        $('#Excluir').click(function () {
            $.ajax(
                {
                    type: "Delete",
                    url: "@Url.Action("Delete", "ViaturaTipo", new { @id = Model.Id})",
                    success: function (data) {
                        if (data.success == true) {
                            window.location.href = '@Url.Action("Index", "ViaturaTipo", new { @notification = true })';
                        }
                        else {
                            $.notify(data.message, "error");
                        }
                    }
                });
        });
    });

</script>